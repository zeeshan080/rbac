[pytest]
asyncio_mode = auto
# Example: Set environment variables for tests if needed
# env =
#   TESTING=True
#   DATABASE_URL_TEST=postgresql+asyncpg://testuser:testpass@localhost/testdb
# For now, we'll assume the main DATABASE_URL is used or tests are designed to clean up.
# A separate test DB is highly recommended for real projects.
